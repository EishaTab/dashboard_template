
"use-client"
import '../../../public/styles/tailwind-sync/tailwind.css'
import React  from 'react';

const LightLayout = ({ children }) => {
    return (
        < >
           {children}
        </>
    );
};

export default LightLayout;