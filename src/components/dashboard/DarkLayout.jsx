"use-client"
import '../../../public/styles/tailwind-dark/tailwind-dark.css'

const DarkLayout = ({ children }) => {

    return (
        < >
         {children}
        </>
    );
};

export default DarkLayout;